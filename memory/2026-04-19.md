- Archiva Admin-/App-Workstream am 2026-04-19 deutlich weitergeführt.
- UI/Workbench-Stand in `archiva/ui.py`:
  - Dokumentauswahl in `/ui/app` bleibt im App-Kontext, Liste bleibt sichtbar, ausgewähltes Dokument wird gehighlightet.
  - Detailseite nur noch per explizitem Button.
  - Rechte Spalte reagiert kontextabhängig: Dokument zeigt Inhaltsvorschau + Metadaten, Cabinet/Register zeigen kompakte Kontextzusammenfassung.
  - Metadatenanzeige rechts nutzt Feldlabels statt interner Namen und ist kompakt zweispaltig gehalten.
  - Vorschau-Route wieder auf `/ui/preview/documents/{id}` statt direkter Download-Route, weil Einbettung über Download sofortigen Download auslöste.
  - Nutzerhinweis bleibt wichtig: Preview-Größe muss real im Browser geprüft werden, reine Außenlayout-Änderungen lösen das Problem nicht sicher.
- Fachliche App-Logik stabilisiert:
  - App arbeitet auf Instanzen, Admin auf Definitionen.
  - Baum zeigt Definitionsfallback auch ohne vorhandene Instanzen.
  - `Register anlegen` nur noch dort, wo der gewählte `CabinetType` tatsächlich `RegisterType`-Definitionen hat.
  - Intake nutzt nur direkte Dokument-Metadaten, keine geerbten CabinetType-/RegisterType-Felder.
  - Cabinet/Register-Kontext bei Intake/Upload wird aus ausgewähltem Knoten bzw. Formular korrekt übernommen.
- Volltext-/Index-Startpunkt aufgebaut:
  - `archiva/config.py` erweitert um `engine`, `opensearch_url`, `index_name`.
  - Neue/erweiterte Komponenten: `archiva/search/mapping.py`, `archiva/indexer/opensearch_client.py`, `archiva/indexer/extractor.py`, `archiva/indexer/ocr.py`, `archiva/indexer/status.py`, `archiva/search_api.py`, `archiva/internal_index_api.py`, `archiva/search_legacy.py`, `archiva/FULLTEXT_ARCHITEKTUR.md`, `archiva/OCR_SETUP.md`.
  - Dokumentablage erzeugt Indexjobs; Worker nutzt Mapping + Textextraktion.
  - Extraktion: Textdateien direkt, PDFs über `pypdf`/`pdftotext`, OCR-Fallback via `ocrmypdf`, Bilder via `tesseract`.
  - Suchservice nutzt zuerst OpenSearch, fällt bei Fehlern auf Postgres-Fallback zurück.
  - UI zeigt Tool-Verfügbarkeit (`tesseract`, `ocrmypdf`, `pdftotext`, `pypdf`) und Dokumente zeigen zusätzlich `index_status`, `index_engine`, `index_ocr_used`.
- Wichtige technische Stolpersteine dieses Tages:
  - Namenskonflikt `archiva/search.py` vs. Paket `archiva/search/`.
  - `archiva.api` ist Datei, kein Package; neue Router mussten auf Top-Level liegen.
  - OpenSearch-Client wurde von `requests` auf Python-Stdlib (`urllib`) umgestellt, um fehlende Abhängigkeit zu vermeiden.
- Offener Punkt nach den letzten Fixes:
  - Es gab mehrere Start-/Importfehler beim Umbau der Suche; nach dem letzten `urllib`-Fix ist der endgültig bestätigte frische Erfolgsstart noch nicht dokumentiert und sollte als Erstes in der nächsten Session geprüft werden.
- Nächster sinnvoller Einstieg in der nächsten Session:
  - `cat /Users/clawdia/.openclaw/workspace/memory/2026-04-19.md /Users/clawdia/.openclaw/workspace/memory/archiva.md`

- Später Nutzerbefund in Archiva-App: Beim Dokumentablegen fehlten erneut die Metadaten-Eingabefelder; Ursache laut Analyse war ein UI-Rückschritt in `archiva/ui.py`, bei dem die bereits aufgebauten Intake-Felder (`capture_fields_html`) im Formular nicht mehr gerendert wurden.
- Danach weiterer Nutzerbefund: Validierung meldet bei Dokumenterfassung `Metadaten ungültig: Value must be unique`, obwohl Beispielwert `2026-003` laut Nutzer noch nicht existiert. Das deutet auf einen Fehler in der Unique-Prüfung für Dokument-Metadaten hin und ist der nächste priorisierte Bug für die Folgesession.
